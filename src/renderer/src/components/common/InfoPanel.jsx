// we'll use react-resizable-panels to create a resizable panel layout component

import React from 'react';
import { Container } from '@mui/material';

export default function InfoPanel({ children }) {
  return (
    <container>
        {children}
    </container>
  );
}

